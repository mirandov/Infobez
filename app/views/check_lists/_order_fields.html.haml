.nested-fields
  %br
  .row
    .col-md-4
      .form-group
        = f.label :service
        = f.select :service_id ,options_for_select((Service.all.map{|t| ["#{t.name}", t.id]}), f.object.service_id), class: 'form-control'
    .col-md-4
      .form-group
        = f.label :person
        = f.text_field :person_id, class: 'form-control'
  = link_to_remove_association "remove order", f
